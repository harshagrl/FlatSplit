// Prisma schema will be fully defined in Phase 2
// This is a placeholder to verify Prisma is configured correctly

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}
